version: "3.9"

services:
  web:
    image: ${IMAGE_NAME:-expensebot-web:latest}
    build:
      context: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - apps/web/.env.production
    volumes:
      - expensebot_data:/data

volumes:
  expensebot_data:
